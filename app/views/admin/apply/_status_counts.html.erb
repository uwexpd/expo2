<% texts = [] %>
<% @offering.statuses.sort.reverse.each do |t| %>
	<% count = @offering.application_for_offerings.with_status(t.application_status_name).size %>
	<% texts << "<b>#{count}</b> #{t.private_title}" unless count.zero? %>
<% end %>
<%= raw texts.join(", ") %>